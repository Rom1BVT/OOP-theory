using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shell : Ammunition
{
    // Start is called before the first frame update
    void Start()
    {
        velocity = 60;
        strikePower = 30;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
